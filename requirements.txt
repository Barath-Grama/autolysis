# Upper bounds are deliberate. Open-ended ">=" pins let a future major release
# silently redefine what "tested" means; the caps below are the majors this
# project has actually been exercised against.
#
# matplotlib is capped below 3.13 specifically: seaborn 0.13.x still calls
# Axes.bxp with the `vert` keyword, which matplotlib deprecated in 3.11 and
# schedules for removal in 3.13. Raise the cap once seaborn ships the fix.
pandas>=2.2,<4
numpy>=1.26,<3
scikit-learn>=1.4,<2
matplotlib>=3.8,<3.13
seaborn>=0.13,<0.15
httpx>=0.27,<1
rich>=13.7,<16
markdown>=3.6,<4
pytest>=8.0,<10
